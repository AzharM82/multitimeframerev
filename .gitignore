node_modules
dist
api/dist
api/node_modules
.env
api/.env
api/local.settings.json
*.local
.vite
*.tsbuildinfo
*.jpg
dump.rdb

# autoresearch — local cache, per-run logs, ledger (regenerable)
autoresearch/.venv/
autoresearch/__pycache__/
autoresearch/data/
autoresearch/.cache/
autoresearch/results.tsv
autoresearch/run.log
autoresearch/baseline.log
autoresearch/reports/draft-*.md

# CI clones tos-reversal-scanner here during weekly-research runs.
# Not part of this repo — never commit it.
tos-reversal-scanner/

# User-only data files / scratch (kept locally, not in repo)
-
*.csv
*.txt
morning_capitulation_scanner_prompt.md
**/__pycache__/
*.pyc
.lavish/
